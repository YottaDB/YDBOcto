{%
#include "physical_plan.h"
#include "template_helpers.h"

TEMPLATE(tmpl_temp_key_advance, SqlKey *key) {
    TEMPLATE_INIT();

    %}$O(^|"{{ config->global_directory }}"|{{ config->global_names.raw_cursor }}(cursorId,"keys",{{ key->random_id|%d }}," "," ",{%
    TMPL(tmpl_key, key);
    %})){%
    
    TEMPLATE_END();
}
%}
