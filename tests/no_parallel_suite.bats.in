#################################################################
#								#
# Copyright (c) 2022-2026 YottaDB LLC and/or its subsidiaries.	#
# All rights reserved.						#
#								#
#	This source code contains the intellectual property	#
#	of its copyright holder(s), and is made available	#
#	under a license.  If you do not know the terms of	#
#	the license, please stop and do not read further.	#
#								#
#################################################################

load test_helpers

setup() {
  init_test
  createdb
}

teardown() {
  corecheck
}

@test "TERR021 : error when CI file does not exist" {
	if [[ @DISABLE_INSTALL@ == "OFF" ]]; then
		# Test only works when DISABLE_INSTALL=ON because then octo is running from ../src and
		# is therefore looking for its ydbocto.ci at ../src/ydbocto.ci
		# It's a no_parallel test because it causes octo to abort, which would disrupt concurrent tests
		skip 'Skipping test as we cannot move ydbocto.ci if it is installed under $ydb_dist'
	fi
	# Now that we know ydbocto.ci is installed under "src" subdirectory, make it disappear temporarily.
	mv ../src/ydbocto.ci call-in.bak
	load_fixture TERR021.sql subtest novv
	# cleanup before the `verify_output` call as if the latter fails, it will cause other tests to fail
	mv call-in.bak ../src/ydbocto.ci
	verify_output TERR021 output.txt
}

