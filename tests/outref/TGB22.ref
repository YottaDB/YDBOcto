
CREATE FUNCTION SAMEVALUE(INTEGER) RETURNS INTEGER AS $$samevalue^functions;
CREATE FUNCTION SAMEVALUE(NUMERIC) RETURNS NUMERIC AS $$samevalue^functions;
CREATE FUNCTION SAMEVALUE(BOOLEAN) RETURNS BOOLEAN AS $$samevalue^functions;
CREATE FUNCTION SAMEVALUE(VARCHAR) RETURNS VARCHAR AS $$samevalue^functions;
CREATE FUNCTION PARMLESSFUNC() RETURNS VARCHAR AS $$^PARMLESSFUNC;

-- Postgres function
-- create function paramlessfunc() returns integer
-- as 'select 1;'
-- LANGUAGE SQL
-- IMMUTABLE;


-- Functions in GroupBy
-- Using id
select samevalue(id) from names;
select samevalue(id) from names group by id;
select samevalue(id) from names group by samevalue(id);
select samevalue(id) from names group by id having 1=samevalue(id);
select samevalue(id) from names group by id having 1=samevalue(id) order by samevalue(id);
select samevalue(id) from names group by id having 1=id;
select samevalue(id) from names group by id having 1=samevalue(id) order by id;
select samevalue(id) from names group by samevalue(id) having 1=samevalue(id);
select samevalue(id) from names group by samevalue(id) having 1=samevalue(id) order by samevalue(id);
select 1 from names group by id having 1=samevalue(id);
select 1 from names group by id having 1=samevalue(id) order by samevalue(id);
select 1 from names group by id having 1=id;
select 1 from names group by id having 1=samevalue(id) order by id;
select 1 from names group by samevalue(id) having 1=samevalue(id);
select 1 from names group by samevalue(id) having 1=samevalue(id) order by samevalue(id);
select samevalue(firstname) from names group by samevalue(id),firstname having 1=samevalue(id) order by samevalue(id);
select samevalue(id) from names group by 1;
select samevalue(id) from names group by 1 order by samevalue(id);
select samevalue(id) from names group by 1 having 1!=samevalue(id) order by samevalue(id);
select samevalue(id) from names group by 1 order by 1;
select samevalue(id) from names group by 1 having 1!=samevalue(id) order by 1;
select samevalue(id) from names group by 1,firstname order by samevalue(firstname);
select samevalue(id) from names group by samevalue(id),id;

-- Using firstname
select samevalue(lastname) from names;
select samevalue(lastname) from names group by lastname;
select samevalue(lastname) from names group by samevalue(lastname);
select samevalue(lastname) from names group by lastname having 'Cool'!=samevalue(lastname);
select samevalue(lastname) from names group by lastname having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
select samevalue(lastname) from names group by lastname having 'Cool'!=lastname;
select samevalue(lastname) from names group by lastname having 'Cool'!=samevalue(lastname) order by lastname;
select samevalue(lastname) from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname);
select samevalue(lastname) from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
select 1 from names group by lastname having 'Cool'!=samevalue(lastname);
select 1 from names group by lastname having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
select 1 from names group by lastname having 'Cool'!=lastname;
select 1 from names group by lastname having 'Cool'!=samevalue(lastname) order by lastname;
select 1 from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname);
select 1 from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
select samevalue(firstname) from names group by samevalue(lastname),firstname having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
select samevalue(lastname) from names group by 1;
select samevalue(lastname) from names group by 1 order by samevalue(lastname);
select samevalue(lastname) from names group by 1 having 'Zero'!=samevalue(lastname) order by samevalue(lastname);
select samevalue(lastname) from names group by 1 order by 1;
select samevalue(lastname) from names group by 1 having 'Zero'!=samevalue(lastname) order by 1;
select samevalue(lastname) from names group by 1,firstname order by samevalue(firstname);
select samevalue(lastname) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
select samevalue(lastname) from names group by 1,firstname having 'Zero'!=firstname order by 1;
select samevalue(lastname) from names group by samevalue(lastname),id;

-- Using Binary and Unary operation along with function operand
select 'test'||samevalue(lastname) from names;
select 'test'||samevalue(lastname) from names group by lastname;
select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by lastname having 'testCool'!='test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by lastname having 'Cool'!=lastname;
select 'test'||samevalue(lastname) from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by lastname;
select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
select 1 from names group by lastname having 'testCool'!='test'||samevalue(lastname);
select 1 from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
select 1 from names group by lastname having 'Cool'!=lastname;
select 1 from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by lastname;
select 1 from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname);
select 1 from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
select samevalue(firstname) from names group by 'test'||samevalue(lastname),firstname having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by 1;
select 'test'||samevalue(lastname) from names group by 1 order by 'test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by 1 having 'Zero'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
select 'test'||samevalue(lastname) from names group by 1 order by 1;
select 'test'||samevalue(lastname) from names group by 1 having 'Zero'!='test'||samevalue(lastname) order by 1;
select 'test'||samevalue(lastname) from names group by 1,firstname order by samevalue(firstname);
select 'test'||samevalue(lastname) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
select 'test'||samevalue(lastname) from names group by 1,firstname having 'Zero'!=firstname order by 1;
select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname),id;
select not 1!=samevalue(id) from names group by not 1!=samevalue(id);
select not 1!=samevalue(id) from names group by id;
select samevalue('test'||samevalue(lastname)) from names group by lastname;
select samevalue('test'||samevalue(lastname)) from names group by samevalue(lastname) order by 'zero'||samevalue(lastname);
select samevalue('test'||samevalue(lastname)) from names group by 'test'||samevalue(lastname);
select samevalue('test'||samevalue(lastname)) from names group by samevalue('test'||samevalue(lastname));


-- Using Binary and Unary operation as function parametersa
select samevalue(NOT 'test'=lastname) from names;
select samevalue(NOT 'test'=lastname) from names group by lastname;
select samevalue(NOT 'test'=lastname) from names group by 'test'=lastname;
select samevalue(NOT 'test'=lastname) from names group by NOT 'test'=lastname;
select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by lastname having 'Cool'!=lastname;
select samevalue(NOT 'test'=lastname) from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by lastname;
select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by NOT 'test'=lastname having FALSE!=(NOT 'test'=lastname);
select 1 from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname);
select 1 from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by 'test'=lastname;
select 1 from names group by lastname having 'Cool'!=lastname;
select 1 from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by lastname;
select 1 from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname);
select 1 from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
select samevalue(firstname) from names group by samevalue(NOT 'test'=lastname),firstname having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by 1;
select samevalue(NOT 'test'=lastname) from names group by 1 order by samevalue(NOT 'test'=lastname);
select samevalue(NOT 'test'=lastname) from names group by 1 order by 1;
select samevalue(NOT 'test'=lastname) from names group by 1,firstname order by samevalue(firstname);
select samevalue(NOT 'test'=lastname) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
select samevalue(NOT 'test'=lastname) from names group by 1,firstname having 'Zero'!=firstname order by 1;
select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname),id;
select not TRUE!=samevalue(NOT 'test'=lastname) from names group by not TRUE!=samevalue(NOT 'test'=lastname);
select not TRUE!=samevalue(NOT 'test'=lastname) from names group by lastname;
select not TRUE!=samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname);
select not TRUE!=samevalue(NOT 'test'=lastname) from names group by TRUE!=samevalue(NOT 'test'=lastname);


-- Parmless function usage
select 1 from names group by parmlessfunc();
select firstname from names group by parmlessfunc(),firstname;
select parmlessfunc() from names group by parmlessfunc(),firstname;
select parmlessfunc(),firstname from names group by parmlessfunc(),firstname;
select parmlessfunc() from names group by parmlessfunc();
select parmlessfunc() from names group by lastname;
select parmlessfunc() from names group by parmlessfunc(),id;
select parmlessfunc()!='Success' from names group by parmlessfunc()!='Success';
select parmlessfunc()!=firstname from names group by parmlessfunc()!=firstname;
select parmlessfunc()!=firstname from names group by firstname;
select parmlessfunc()!=firstname from names group by parmlessfunc()!=firstname,firstname;
select ('Zero'!=lastname)!=(parmlessfunc()!=firstname) from names group by firstname,lastname;
select ('Zero'!=lastname)!=(parmlessfunc()!=firstname) from names group by ('Zero'!=lastname)!=(parmlessfunc()!=firstname);
select parmlessfunc() from names group by 1;
select parmlessfunc() from names group by 1 order by 1;
select parmlessfunc() from names group by parmlessfunc() order by 1;
select parmlessfunc() from names group by 1,firstname having parmlessfunc()!=firstname;
select parmlessfunc() from names group by 1 having parmlessfunc()!='Zero';
select parmlessfunc() from names group by 1 order by parmlessfunc();
select parmlessfunc() from names group by 1 order by parmlessfunc()!='Zero';
select parmlessfunc() from names group by lastname;
select parmlessfunc() from names group by lastname order by lastname!=parmlessfunc();
select 1 from names group by parmlessfunc();
select 1 from names group by parmlessfunc() order by parmlessfunc();
select 1 from names group by lastname order by parmlessfunc();
select 1 from names group by lastname having parmlessfunc()!=lastname;
select 1 from names group by lastname having parmlessfunc()!=lastname order by parmlessfunc();
select parmlessfunc() from names group by lastname order by lastname!=parmlessfunc();
select parmlessfunc() from names group by lastname having lastname!=parmlessfunc();

-- function_call_STATEMENT with CAST spec and CAST expr
-- function with CAST_EXPRESSION
select samevalue(CAST(id AS NUMERIC)) from names;
select samevalue(CAST(id AS NUMERIC)) from names group by id;
select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS INTEGER)),id;
select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS INTEGER));
select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=id;
select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by id;
select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
select 1 from names group by id having 1=samevalue(CAST(id AS NUMERIC));
select 1 from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
select 1 from names group by id having 1=id;
select 1 from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by id;
select 1 from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC));
select 1 from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
select samevalue(firstname) from names group by samevalue(CAST(id AS NUMERIC)),firstname having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by 1;
select samevalue(CAST(id AS NUMERIC)) from names group by 1 order by samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by 1 having 1!=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
select samevalue(CAST(id AS NUMERIC)) from names group by 1 order by 1;
select samevalue(CAST(id AS NUMERIC)) from names group by 1 having 1!=samevalue(CAST(id AS NUMERIC)) order by 1;
select samevalue(CAST(id AS NUMERIC)) from names group by 1,firstname order by samevalue(firstname);
select samevalue(CAST(id AS NUMERIC)) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC)),id;

-- function with CAST_SPECIFICATION
select samevalue(id::NUMERIC) from names;
select samevalue(id::NUMERIC) from names group by id;
select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by samevalue(id::INTEGER),id;
select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::INTEGER);
select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by id having 1=id;
select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::NUMERIC) order by id;
select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
select 1 from names group by id having 1=samevalue(id::NUMERIC);
select 1 from names group by id having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
select 1 from names group by id having 1=id;
select 1 from names group by id having 1=samevalue(id::NUMERIC) order by id;
select 1 from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC);
select 1 from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
select samevalue(firstname) from names group by samevalue(id::NUMERIC),firstname having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by 1;
select samevalue(id::NUMERIC) from names group by 1 order by samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by 1 having 1!=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
select samevalue(id::NUMERIC) from names group by 1 order by 1;
select samevalue(id::NUMERIC) from names group by 1 having 1!=samevalue(id::NUMERIC) order by 1;
select samevalue(id::NUMERIC) from names group by 1,firstname order by samevalue(firstname);
select samevalue(id::NUMERIC) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC),id;

-- With CAST_EXPRESSION usage
select samevalue(firstname) from names group by CAST(id AS NUMERIC),firstname having 1=CAST(id AS NUMERIC) order by CAST(id AS NUMERIC);
select CAST(id AS NUMERIC) from names group by 1,firstname order by samevalue(firstname);
select CAST(id AS NUMERIC) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;

-- With CAST_SPECIFICATION
select samevalue(firstname) from names group by id::NUMERIC,firstname having 1=id::NUMERIC order by id::NUMERIC;
select id::NUMERIC from names group by 1,firstname order by samevalue(firstname);
select id::NUMERIC from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;

-- Using Binary and Unary operation along with function operand and cast speci
select 1+samevalue(id)::INTEGER from names group by 1+samevalue(id)::INTEGER;
select 1+samevalue(id)::INTEGER from names group by 1;
select 1+samevalue(id)::INTEGER from names group by 1 having 1+samevalue(id)::INTEGER != 2;
select 1+CAST(samevalue(id) as INTEGER) from names group by 1+CAST(samevalue(id) as INTEGER);

-- PARMLESSFUNC() returns "SUCCESS"
select parmlessfunc() from names group by parmlessfunc()::VARCHAR;
select parmlessfunc() from names group by parmlessfunc()::VARCHAR having 'FAIL'!=parmlessfunc()::VARCHAR;
select parmlessfunc() from names group by parmlessfunc()::VARCHAR having 'FAIL'!=parmlessfunc()::VARCHAR;
select parmlessfunc() from names group by 1 having 'FAIL'!=parmlessfunc()::VARCHAR;
select parmlessfunc() from names group by 1 having 'FAIL'!=parmlessfunc()::VARCHAR;
select parmlessfunc() from names group by CAST(parmlessfunc() as VARCHAR);
select parmlessfunc() from names group by CAST(parmlessfunc() as VARCHAR) having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
select parmlessfunc() from names group by CAST(parmlessfunc() as VARCHAR) having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
select parmlessfunc() from names group by 1 having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
select parmlessfunc() from names group by 1 having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);

-- Using string literals
SELECT 'test' FROM names GROUP BY 1;
SELECT 'test' FROM names GROUP BY 1 HAVING 'test'!='hello';
SELECT 'test' FROM names GROUP BY firstname HAVING 'test'!='hello' ORDER BY 1;
OCTO> CREATE FUNCTION SAMEVALUE(INTEGER) RETURNS INTEGER AS $$samevalue^functions;
CREATE FUNCTION
OCTO> CREATE FUNCTION SAMEVALUE(NUMERIC) RETURNS NUMERIC AS $$samevalue^functions;
CREATE FUNCTION
OCTO> CREATE FUNCTION SAMEVALUE(BOOLEAN) RETURNS BOOLEAN AS $$samevalue^functions;
CREATE FUNCTION
OCTO> CREATE FUNCTION SAMEVALUE(VARCHAR) RETURNS VARCHAR AS $$samevalue^functions;
CREATE FUNCTION
OCTO> CREATE FUNCTION PARMLESSFUNC() RETURNS VARCHAR AS $$^PARMLESSFUNC;
CREATE FUNCTION
OCTO> select samevalue(id) from names;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id) from names group by id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id) from names group by samevalue(id);
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id) from names group by id having 1=samevalue(id);
samevalue
1
(1 row)
OCTO> select samevalue(id) from names group by id having 1=samevalue(id) order by samevalue(id);
samevalue
1
(1 row)
OCTO> select samevalue(id) from names group by id having 1=id;
samevalue
1
(1 row)
OCTO> select samevalue(id) from names group by id having 1=samevalue(id) order by id;
samevalue
1
(1 row)
OCTO> select samevalue(id) from names group by samevalue(id) having 1=samevalue(id);
samevalue
1
(1 row)
OCTO> select samevalue(id) from names group by samevalue(id) having 1=samevalue(id) order by samevalue(id);
samevalue
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(id);
???
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(id) order by samevalue(id);
???
1
(1 row)
OCTO> select 1 from names group by id having 1=id;
???
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(id) order by id;
???
1
(1 row)
OCTO> select 1 from names group by samevalue(id) having 1=samevalue(id);
???
1
(1 row)
OCTO> select 1 from names group by samevalue(id) having 1=samevalue(id) order by samevalue(id);
???
1
(1 row)
OCTO> select samevalue(firstname) from names group by samevalue(id),firstname having 1=samevalue(id) order by samevalue(id);
samevalue
Acid
(1 row)
OCTO> select samevalue(id) from names group by 1;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id) from names group by 1 order by samevalue(id);
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id) from names group by 1 having 1!=samevalue(id) order by samevalue(id);
samevalue
0
2
3
4
5
(5 rows)
OCTO> select samevalue(id) from names group by 1 order by 1;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id) from names group by 1 having 1!=samevalue(id) order by 1;
samevalue
0
2
3
4
5
(5 rows)
OCTO> select samevalue(id) from names group by 1,firstname order by samevalue(firstname);
samevalue
1
2
4
3
0
5
(6 rows)
OCTO> select samevalue(id) from names group by samevalue(id),id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(lastname) from names;
samevalue
Cool
Burn
Killer
Nikon

Cool
(6 rows)
OCTO> select samevalue(lastname) from names group by lastname;
samevalue

Burn
Cool
Nikon
Killer
(5 rows)
OCTO> select samevalue(lastname) from names group by samevalue(lastname);
samevalue

Burn
Cool
Nikon
Killer
(5 rows)
OCTO> select samevalue(lastname) from names group by lastname having 'Cool'!=samevalue(lastname);
samevalue
Burn
Nikon
Killer
(3 rows)
OCTO> select samevalue(lastname) from names group by lastname having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
samevalue
Burn
Killer
Nikon
(3 rows)
OCTO> select samevalue(lastname) from names group by lastname having 'Cool'!=lastname;
samevalue
Burn
Nikon
Killer
(3 rows)
OCTO> select samevalue(lastname) from names group by lastname having 'Cool'!=samevalue(lastname) order by lastname;
samevalue
Burn
Killer
Nikon
(3 rows)
OCTO> select samevalue(lastname) from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname);
samevalue
Burn
Nikon
Killer
(3 rows)
OCTO> select samevalue(lastname) from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
samevalue
Burn
Killer
Nikon
(3 rows)
OCTO> select 1 from names group by lastname having 'Cool'!=samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having 'Cool'!=lastname;
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having 'Cool'!=samevalue(lastname) order by lastname;
???
1
1
1
(3 rows)
OCTO> select 1 from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select 1 from names group by samevalue(lastname) having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select samevalue(firstname) from names group by samevalue(lastname),firstname having 'Cool'!=samevalue(lastname) order by samevalue(lastname);
samevalue
Acid
Cereal
Lord
(3 rows)
OCTO> select samevalue(lastname) from names group by 1;
samevalue

Burn
Cool
Nikon
Killer
(5 rows)
OCTO> select samevalue(lastname) from names group by 1 order by samevalue(lastname);
samevalue
Burn
Cool
Killer
Nikon

(5 rows)
OCTO> select samevalue(lastname) from names group by 1 having 'Zero'!=samevalue(lastname) order by samevalue(lastname);
samevalue
Burn
Cool
Killer
Nikon
(4 rows)
OCTO> select samevalue(lastname) from names group by 1 order by 1;
samevalue
Burn
Cool
Killer
Nikon

(5 rows)
OCTO> select samevalue(lastname) from names group by 1 having 'Zero'!=samevalue(lastname) order by 1;
samevalue
Burn
Cool
Killer
Nikon
(4 rows)
OCTO> select samevalue(lastname) from names group by 1,firstname order by samevalue(firstname);
samevalue
Burn
Killer

Nikon
Cool
(5 rows)
OCTO> select samevalue(lastname) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
samevalue
Burn
Killer
Nikon

(4 rows)
OCTO> select samevalue(lastname) from names group by 1,firstname having 'Zero'!=firstname order by 1;
samevalue
Burn
Killer
Nikon

(4 rows)
OCTO> select samevalue(lastname) from names group by samevalue(lastname),id;
samevalue

Burn
Cool
Cool
Nikon
Killer
(6 rows)
OCTO> select 'test'||samevalue(lastname) from names;
???
testCool
testBurn
testKiller
testNikon

testCool
(6 rows)
OCTO> select 'test'||samevalue(lastname) from names group by lastname;
???

testBurn
testCool
testNikon
testKiller
(5 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname);
???

testBurn
testCool
testNikon
testKiller
(5 rows)
OCTO> select 'test'||samevalue(lastname) from names group by lastname having 'testCool'!='test'||samevalue(lastname);
???
testBurn
testNikon
testKiller
(3 rows)
OCTO> select 'test'||samevalue(lastname) from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
???
testBurn
testKiller
testNikon
(3 rows)
OCTO> select 'test'||samevalue(lastname) from names group by lastname having 'Cool'!=lastname;
???
testBurn
testNikon
testKiller
(3 rows)
OCTO> select 'test'||samevalue(lastname) from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by lastname;
???
testBurn
testKiller
testNikon
(3 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname);
???
testBurn
testNikon
testKiller
(3 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
???
testBurn
testKiller
testNikon
(3 rows)
OCTO> select 1 from names group by lastname having 'testCool'!='test'||samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having 'Cool'!=lastname;
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having 'testCool'!='test'||samevalue(lastname) order by lastname;
???
1
1
1
(3 rows)
OCTO> select 1 from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select 1 from names group by 'test'||samevalue(lastname) having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
???
1
1
1
(3 rows)
OCTO> select samevalue(firstname) from names group by 'test'||samevalue(lastname),firstname having 'testCool'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
samevalue
Acid
Cereal
Lord
(3 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1;
???

testBurn
testCool
testNikon
testKiller
(5 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1 order by 'test'||samevalue(lastname);
???
testBurn
testCool
testKiller
testNikon

(5 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1 having 'Zero'!='test'||samevalue(lastname) order by 'test'||samevalue(lastname);
???
testBurn
testCool
testKiller
testNikon
(4 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1 order by 1;
???
testBurn
testCool
testKiller
testNikon

(5 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1 having 'Zero'!='test'||samevalue(lastname) order by 1;
???
testBurn
testCool
testKiller
testNikon
(4 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1,firstname order by samevalue(firstname);
???
testBurn
testKiller

testNikon
testCool
(5 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
???
testBurn
testKiller
testNikon

(4 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 1,firstname having 'Zero'!=firstname order by 1;
???
testBurn
testKiller
testNikon

(4 rows)
OCTO> select 'test'||samevalue(lastname) from names group by 'test'||samevalue(lastname),id;
???

testBurn
testCool
testCool
testNikon
testKiller
(6 rows)
OCTO> select not 1!=samevalue(id) from names group by not 1!=samevalue(id);
???
0
1
(2 rows)
OCTO> select not 1!=samevalue(id) from names group by id;
???
0
1
0
0
0
0
(6 rows)
OCTO> select samevalue('test'||samevalue(lastname)) from names group by lastname;
samevalue

testBurn
testCool
testNikon
testKiller
(5 rows)
OCTO> select samevalue('test'||samevalue(lastname)) from names group by samevalue(lastname) order by 'zero'||samevalue(lastname);
samevalue
testBurn
testCool
testKiller
testNikon

(5 rows)
OCTO> select samevalue('test'||samevalue(lastname)) from names group by 'test'||samevalue(lastname);
samevalue

testBurn
testCool
testNikon
testKiller
(5 rows)
OCTO> select samevalue('test'||samevalue(lastname)) from names group by samevalue('test'||samevalue(lastname));
samevalue

testBurn
testCool
testNikon
testKiller
(5 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names;
samevalue
1
1
1
1

1
(6 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by lastname;
samevalue

1
1
1
1
(5 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 'test'=lastname;
samevalue

1
(2 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by NOT 'test'=lastname;
samevalue

1
(2 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname);
samevalue

1
(2 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname);
samevalue
1
1
1
1
(4 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
samevalue
1
1
1
1
(4 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by lastname having 'Cool'!=lastname;
samevalue
1
1
1
(3 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by lastname;
samevalue
1
1
1
1
(4 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname);
samevalue
1
(1 row)
OCTO> select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
samevalue
1
(1 row)
OCTO> select samevalue(NOT 'test'=lastname) from names group by NOT 'test'=lastname having FALSE!=(NOT 'test'=lastname);
samevalue
1
(1 row)
OCTO> select 1 from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname);
???
1
1
1
1
(4 rows)
OCTO> select 1 from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by 'test'=lastname;
???
1
1
1
1
(4 rows)
OCTO> select 1 from names group by lastname having 'Cool'!=lastname;
???
1
1
1
(3 rows)
OCTO> select 1 from names group by lastname having FALSE!=samevalue(NOT 'test'=lastname) order by lastname;
???
1
1
1
1
(4 rows)
OCTO> select 1 from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname);
???
1
(1 row)
OCTO> select 1 from names group by samevalue(NOT 'test'=lastname) having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
???
1
(1 row)
OCTO> select samevalue(firstname) from names group by samevalue(NOT 'test'=lastname),firstname having FALSE!=samevalue(NOT 'test'=lastname) order by samevalue(NOT 'test'=lastname);
samevalue
Acid
Lord
Zero
Cereal
(4 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 1;
samevalue

1
(2 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 1 order by samevalue(NOT 'test'=lastname);
samevalue
1

(2 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 1 order by 1;
samevalue
1

(2 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 1,firstname order by samevalue(firstname);
samevalue
1
1

1
1
(5 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
samevalue
1
1
1

(4 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by 1,firstname having 'Zero'!=firstname order by 1;
samevalue
1
1
1

(4 rows)
OCTO> select samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname),id;
samevalue

1
1
1
1
1
(6 rows)
OCTO> select not TRUE!=samevalue(NOT 'test'=lastname) from names group by not TRUE!=samevalue(NOT 'test'=lastname);
???

1
(2 rows)
OCTO> select not TRUE!=samevalue(NOT 'test'=lastname) from names group by lastname;
???

1
1
1
1
(5 rows)
OCTO> select not TRUE!=samevalue(NOT 'test'=lastname) from names group by samevalue(NOT 'test'=lastname);
???

1
(2 rows)
OCTO> select not TRUE!=samevalue(NOT 'test'=lastname) from names group by TRUE!=samevalue(NOT 'test'=lastname);
???

1
(2 rows)
OCTO> select 1 from names group by parmlessfunc();
???
1
(1 row)
OCTO> select firstname from names group by parmlessfunc(),firstname;
firstname
Acid
Joey
Lord
Zero
Cereal
(5 rows)
OCTO> select parmlessfunc() from names group by parmlessfunc(),firstname;
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(5 rows)
OCTO> select parmlessfunc(),firstname from names group by parmlessfunc(),firstname;
parmlessfunc|firstname
SUCCESS|Acid
SUCCESS|Joey
SUCCESS|Lord
SUCCESS|Zero
SUCCESS|Cereal
(5 rows)
OCTO> select parmlessfunc() from names group by parmlessfunc();
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by lastname;
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(5 rows)
OCTO> select parmlessfunc() from names group by parmlessfunc(),id;
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(6 rows)
OCTO> select parmlessfunc()!='Success' from names group by parmlessfunc()!='Success';
???
1
(1 row)
OCTO> select parmlessfunc()!=firstname from names group by parmlessfunc()!=firstname;
???
1
(1 row)
OCTO> select parmlessfunc()!=firstname from names group by firstname;
???
1
1
1
1
1
(5 rows)
OCTO> select parmlessfunc()!=firstname from names group by parmlessfunc()!=firstname,firstname;
???
1
1
1
1
1
(5 rows)
OCTO> select ('Zero'!=lastname)!=(parmlessfunc()!=firstname) from names group by firstname,lastname;
???
0

0
0
0
(5 rows)
OCTO> select ('Zero'!=lastname)!=(parmlessfunc()!=firstname) from names group by ('Zero'!=lastname)!=(parmlessfunc()!=firstname);
???

0
(2 rows)
OCTO> select parmlessfunc() from names group by 1;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 order by 1;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by parmlessfunc() order by 1;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1,firstname having parmlessfunc()!=firstname;
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(5 rows)
OCTO> select parmlessfunc() from names group by 1 having parmlessfunc()!='Zero';
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 order by parmlessfunc();
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 order by parmlessfunc()!='Zero';
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by lastname;
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(5 rows)
OCTO> select parmlessfunc() from names group by lastname order by lastname!=parmlessfunc();
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(5 rows)
OCTO> select 1 from names group by parmlessfunc();
???
1
(1 row)
OCTO> select 1 from names group by parmlessfunc() order by parmlessfunc();
???
1
(1 row)
OCTO> select 1 from names group by lastname order by parmlessfunc();
???
1
1
1
1
1
(5 rows)
OCTO> select 1 from names group by lastname having parmlessfunc()!=lastname;
???
1
1
1
1
(4 rows)
OCTO> select 1 from names group by lastname having parmlessfunc()!=lastname order by parmlessfunc();
???
1
1
1
1
(4 rows)
OCTO> select parmlessfunc() from names group by lastname order by lastname!=parmlessfunc();
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(5 rows)
OCTO> select parmlessfunc() from names group by lastname having lastname!=parmlessfunc();
parmlessfunc
SUCCESS
SUCCESS
SUCCESS
SUCCESS
(4 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC));
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS INTEGER)),id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS NUMERIC));
samevalue
1
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS INTEGER));
samevalue
1
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
samevalue
1
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=id;
samevalue
1
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by id;
samevalue
1
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC));
samevalue
1
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
samevalue
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(CAST(id AS NUMERIC));
???
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
???
1
(1 row)
OCTO> select 1 from names group by id having 1=id;
???
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(CAST(id AS NUMERIC)) order by id;
???
1
(1 row)
OCTO> select 1 from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC));
???
1
(1 row)
OCTO> select 1 from names group by samevalue(CAST(id AS NUMERIC)) having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
???
1
(1 row)
OCTO> select samevalue(firstname) from names group by samevalue(CAST(id AS NUMERIC)),firstname having 1=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
samevalue
Acid
(1 row)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1 order by samevalue(CAST(id AS NUMERIC));
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1 having 1!=samevalue(CAST(id AS NUMERIC)) order by samevalue(CAST(id AS NUMERIC));
samevalue
0
2
3
4
5
(5 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1 order by 1;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1 having 1!=samevalue(CAST(id AS NUMERIC)) order by 1;
samevalue
0
2
3
4
5
(5 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1,firstname order by samevalue(firstname);
samevalue
1
2
4
3
0
5
(6 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
samevalue
1
2
3
4
(4 rows)
OCTO> select samevalue(CAST(id AS NUMERIC)) from names group by samevalue(CAST(id AS NUMERIC)),id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC);
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by samevalue(id::INTEGER),id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::NUMERIC);
samevalue
1
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::INTEGER);
samevalue
1
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
samevalue
1
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by id having 1=id;
samevalue
1
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by id having 1=samevalue(id::NUMERIC) order by id;
samevalue
1
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC);
samevalue
1
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
samevalue
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(id::NUMERIC);
???
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
???
1
(1 row)
OCTO> select 1 from names group by id having 1=id;
???
1
(1 row)
OCTO> select 1 from names group by id having 1=samevalue(id::NUMERIC) order by id;
???
1
(1 row)
OCTO> select 1 from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC);
???
1
(1 row)
OCTO> select 1 from names group by samevalue(id::NUMERIC) having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
???
1
(1 row)
OCTO> select samevalue(firstname) from names group by samevalue(id::NUMERIC),firstname having 1=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
samevalue
Acid
(1 row)
OCTO> select samevalue(id::NUMERIC) from names group by 1;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by 1 order by samevalue(id::NUMERIC);
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by 1 having 1!=samevalue(id::NUMERIC) order by samevalue(id::NUMERIC);
samevalue
0
2
3
4
5
(5 rows)
OCTO> select samevalue(id::NUMERIC) from names group by 1 order by 1;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by 1 having 1!=samevalue(id::NUMERIC) order by 1;
samevalue
0
2
3
4
5
(5 rows)
OCTO> select samevalue(id::NUMERIC) from names group by 1,firstname order by samevalue(firstname);
samevalue
1
2
4
3
0
5
(6 rows)
OCTO> select samevalue(id::NUMERIC) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
samevalue
1
2
3
4
(4 rows)
OCTO> select samevalue(id::NUMERIC) from names group by samevalue(id::NUMERIC),id;
samevalue
0
1
2
3
4
5
(6 rows)
OCTO> select samevalue(firstname) from names group by CAST(id AS NUMERIC),firstname having 1=CAST(id AS NUMERIC) order by CAST(id AS NUMERIC);
samevalue
Acid
(1 row)
OCTO> select CAST(id AS NUMERIC) from names group by 1,firstname order by samevalue(firstname);
id
1
2
4
3
0
5
(6 rows)
OCTO> select CAST(id AS NUMERIC) from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
id
1
2
3
4
(4 rows)
OCTO> select samevalue(firstname) from names group by id::NUMERIC,firstname having 1=id::NUMERIC order by id::NUMERIC;
samevalue
Acid
(1 row)
OCTO> select id::NUMERIC from names group by 1,firstname order by samevalue(firstname);
id
1
2
4
3
0
5
(6 rows)
OCTO> select id::NUMERIC from names group by 1,firstname having 'Zero'!=samevalue(firstname) order by 1;
id
1
2
3
4
(4 rows)
OCTO> select 1+samevalue(id)::INTEGER from names group by 1+samevalue(id)::INTEGER;
???
1
2
3
4
5
6
(6 rows)
OCTO> select 1+samevalue(id)::INTEGER from names group by 1;
???
1
2
3
4
5
6
(6 rows)
OCTO> select 1+samevalue(id)::INTEGER from names group by 1 having 1+samevalue(id)::INTEGER != 2;
???
1
3
4
5
6
(5 rows)
OCTO> select 1+CAST(samevalue(id) as INTEGER) from names group by 1+CAST(samevalue(id) as INTEGER);
???
1
2
3
4
5
6
(6 rows)
OCTO> select parmlessfunc() from names group by parmlessfunc()::VARCHAR;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by parmlessfunc()::VARCHAR having 'FAIL'!=parmlessfunc()::VARCHAR;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by parmlessfunc()::VARCHAR having 'FAIL'!=parmlessfunc()::VARCHAR;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 having 'FAIL'!=parmlessfunc()::VARCHAR;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 having 'FAIL'!=parmlessfunc()::VARCHAR;
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by CAST(parmlessfunc() as VARCHAR);
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by CAST(parmlessfunc() as VARCHAR) having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by CAST(parmlessfunc() as VARCHAR) having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
parmlessfunc
SUCCESS
(1 row)
OCTO> select parmlessfunc() from names group by 1 having 'FAIL'!=CAST(parmlessfunc() as VARCHAR);
parmlessfunc
SUCCESS
(1 row)
OCTO> SELECT 'test' FROM names GROUP BY 1;
???
test
(1 row)
OCTO> SELECT 'test' FROM names GROUP BY 1 HAVING 'test'!='hello';
???
test
(1 row)
OCTO> SELECT 'test' FROM names GROUP BY firstname HAVING 'test'!='hello' ORDER BY 1;
???
test
test
test
test
test
(5 rows)
OCTO> 
