#################################################################
#								#
# Copyright (c) 2019 YottaDB LLC and/or its subsidiaries.	#
# All rights reserved.						#
#								#
#	This source code contains the intellectual property	#
#	of its copyright holder(s), and is made available	#
#	under a license.  If you do not know the terms of	#
#	the license, please stop and do not read further.	#
#								#
#################################################################

load test_helpers

setup() {
  init_test
  createdb
  load_fixture customers.sql
  load_fixture customers.zwr
  load_fixture pastas.sql
  load_fixture pastas.zwr
}

@test "TQG01 : Test Query Generator against customers database" {
  cp @PROJECT_SOURCE_DIR@/tests/fixtures/QueryGenerator.m .

  numQueries=150
  prefix="TQG01-"
  $ydb_dist/mumps -run QueryGenerator customers.sql customers.zwr "$numQueries" "$prefix"

  count=1
  while [ $count -le $numQueries ]
  do
    queryfile="${prefix}${count}.sql"
    run_query_in_octo_and_postgres_and_crosscheck customers "$queryfile" "remove_empty_line_at_head"
    count=`expr $count + 1`
  done
}

@test "TQG02 : Test Query Generator against pastas database" {
  cp @PROJECT_SOURCE_DIR@/tests/fixtures/QueryGenerator.m .

  numQueries=150
  prefix="TQG02-"
  $ydb_dist/mumps -run QueryGenerator pastas.sql pastas.zwr "$numQueries" "$prefix"

  count=1
  while [ $count -le $numQueries ]
  do
    queryfile="${prefix}${count}.sql"
    run_query_in_octo_and_postgres_and_crosscheck pastas "$queryfile" "remove_empty_line_at_head"
    count=`expr $count + 1`
  done
}

# #FUTURE_TODO: Add subtests for names and northwind database
